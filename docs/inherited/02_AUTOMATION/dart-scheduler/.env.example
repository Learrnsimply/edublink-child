# Dart scheduler secrets — TEMPLATE (safe to commit; no real values).
# On the VPS: copy to /opt/dart-scheduler/.env and fill real values, then:
#   chmod 600 /opt/dart-scheduler/.env
# send.py loads this file automatically (and falls back to process env).
# The real .env is gitignored — never commit real secrets here.

MAUTIC_USER=omar
MAUTIC_PASS=__set_on_server__
BREVO_KEY=__set_on_server__
TG_TOKEN=__set_on_server__
TG_CHAT=6726176133
