    <!-- ===== PROMO BANNER ===== -->
    <div class="promo-banner" id="dartPromoBanner" data-deadline="">
      <div class="promo-content">
        <div class="promo-text">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"/>
            <path d="M8 12h8M12 8v8" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
          <span>خصم <strong>50%</strong> على كورس دارت — أول خطوة لبناء التطبيقات</span>
          <a href="https://learrnsimply.com/courses/dart/" class="promo-link">اشترك الآن ←</a>
        </div>

        <!-- Countdown Timer -->
        <div class="promo-countdown" id="dartCountdown" aria-label="الوقت المتبقي على العرض">
          <div class="countdown-box">
            <span class="countdown-num" data-unit="days">00</span>
            <span class="countdown-label">يوم</span>
          </div>
          <span class="countdown-sep">:</span>
          <div class="countdown-box">
            <span class="countdown-num" data-unit="hours">00</span>
            <span class="countdown-label">ساعة</span>
          </div>
          <span class="countdown-sep">:</span>
          <div class="countdown-box">
            <span class="countdown-num" data-unit="minutes">00</span>
            <span class="countdown-label">دقيقة</span>
          </div>
          <span class="countdown-sep">:</span>
          <div class="countdown-box">
            <span class="countdown-num" data-unit="seconds">00</span>
            <span class="countdown-label">ثانية</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ===== START: Hero Section ===== -->
    <section class="hero-wrapper hero-improved">
      <div class="hero-container">
        <div class="hero-inner">
          <div class="hero-background">
            <div class="hero-content-wrapper">
              <!-- Main Content -->
              <div class="hero-main-grid">
                <!-- Left Side - Content -->
                <div class="hero-left-content">
                  <div class="hero-badge">
                    <span>اتعلم ببساطة</span>
                  </div>

                  <h1 class="hero-main-title">
                    البرمجة أسهل ممــا تتخيل...
                    <br>
                    لو اتعلمتها ببساطة
                  </h1>

                  <p class="hero-description">
                    ابدأ من الصفر، افهم الأساس صح، واشتغل بإيدك على مشاريع حقيقية من غير تعقيد
                  </p>

                  <!-- Primary CTA (visible immediately) -->
                  <div class="hero-cta-inline">
                    <a href="{{ courses_archive_url }}" class="hero-cta-btn-large">
                      <span>ابدأ الآن</span>
                      <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                        <path d="M4 10H16M16 10L11 5M16 10L11 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </a>
                  </div>

                  <!-- Features List -->
                  <div class="hero-features">
                    <div class="feature-item">
                      <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                        <path d="M16.6666 5L7.49998 14.1667L3.33331 10" stroke="#18A963" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                      <span>شهادة إكمال لكل مستوى</span>
                    </div>
                    <div class="feature-item">
                      <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                        <path d="M16.6666 5L7.49998 14.1667L3.33331 10" stroke="#18A963" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                      <span>مشاريع تطبيقية عملية</span>
                    </div>
                    <div class="feature-item">
                      <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                        <path d="M16.6666 5L7.49998 14.1667L3.33331 10" stroke="#18A963" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                      <span>تمارين بعد كل درس</span>
                    </div>
                    <div class="feature-item">
                      <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                        <path d="M16.6666 5L7.49998 14.1667L3.33331 10" stroke="#18A963" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                      <span>شرح عربي واضح وبسيط</span>
                    </div>
                  </div>

                  <!-- Guarantee -->
                  <div class="hero-guarantee-box">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none">
                      <path d="M12 2l8 4v6c0 5-3.5 8.5-8 10-4.5-1.5-8-5-8-10V6l8-4z" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
                      <path d="M9 12l2 2 4-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                    <span>ضمان استرداد الفلوس خلال 7 أيام على كل الكورسات</span>
                  </div>
                </div>

                <!-- Right Side - Stats -->
                <div class="hero-stats-section">
                  <div class="stats-card">
                    <div class="stat-box">
                      <div class="stat-number">
                        <span class="counter" data-target="10000">0</span>
                        <span class="stat-suffix">+</span>
                      </div>
                      <p class="stat-text">طالب تعلموا من المنصة</p>
                    </div>

                    <div class="stat-box">
                      <div class="stat-number">
                        <span class="counter-20m" data-target="20">20</span>
                      </div>
                      <p class="stat-text">مليون مشاهدة على اليوتيوب</p>
                    </div>

                    <div class="stat-box">
                      <div class="stat-number">
                        <span class="counter-380k" data-target="380">380</span>
                        <span class="stat-suffix">K+</span>
                      </div>
                      <p class="stat-text">مشترك على اليوتيوب</p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const counters = document.querySelectorAll('.counter');
        const observerOptions = {
          threshold: 0.1,
          rootMargin: '0px'
        };

        const observer = new IntersectionObserver(function(entries) {
          entries.forEach(entry => {
            if (entry.isIntersecting && !entry.target.classList.contains('animated')) {
              entry.target.classList.add('animated');
              const target = parseInt(entry.target.getAttribute('data-target'));
              const element = entry.target;
              
              let current = 0;
              const increment = target / 50;
              const timer = setInterval(() => {
                current += increment;
                if (current >= target) {
                  element.textContent = target.toLocaleString('en-US');
                  clearInterval(timer);
                } else {
                  element.textContent = Math.floor(current).toLocaleString('en-US');
                }
              }, 30);
            }
          });
        }, observerOptions);

        counters.forEach(counter => observer.observe(counter));
      });
    </script>

    <!-- Countdown Timer Logic -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        var banner = document.getElementById('dartPromoBanner');
        var countdown = document.getElementById('dartCountdown');
        if (!banner || !countdown) return;

        // Deadline: use data-deadline if set, otherwise a rolling 3-day window
        // persisted per-visitor in localStorage so it feels like a real offer.
        var deadline;
        var attr = (banner.getAttribute('data-deadline') || '').trim();
        if (attr) {
          deadline = new Date(attr).getTime();
        } else {
          var stored = localStorage.getItem('dartOfferDeadline');
          if (stored) {
            deadline = parseInt(stored, 10);
          } else {
            deadline = Date.now() + 3 * 24 * 60 * 60 * 1000; // 3 days
            localStorage.setItem('dartOfferDeadline', deadline);
          }
        }

        var nums = {};
        countdown.querySelectorAll('.countdown-num').forEach(function(el) {
          nums[el.getAttribute('data-unit')] = el;
        });

        function pad(n) { return (n < 10 ? '0' : '') + n; }

        function tick() {
          var diff = deadline - Date.now();
          if (diff <= 0) {
            // Offer ended — reset to a fresh window so the banner stays live.
            deadline = Date.now() + 3 * 24 * 60 * 60 * 1000;
            localStorage.setItem('dartOfferDeadline', deadline);
            diff = deadline - Date.now();
          }
          var days = Math.floor(diff / (1000 * 60 * 60 * 24));
          var hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
          var minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
          var seconds = Math.floor((diff % (1000 * 60)) / 1000);
          if (nums.days) nums.days.textContent = pad(days);
          if (nums.hours) nums.hours.textContent = pad(hours);
          if (nums.minutes) nums.minutes.textContent = pad(minutes);
          if (nums.seconds) nums.seconds.textContent = pad(seconds);
        }

        tick();
        setInterval(tick, 1000);
      });
    </script>

    <style>
      .promo-banner {
        background: linear-gradient(135deg, #1e3a8a 0%, #2E5CC8 45%, #4077F3 100%);
        padding: 14px 24px;
        color: white;
        font-size: 15px;
        font-weight: 500;
        border-bottom: 1px solid rgba(255, 255, 255, 0.12);
      }

      .promo-content {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 20px;
        flex-wrap: wrap;
        max-width: 1200px;
        margin: 0 auto;
      }

      .promo-text {
        display: flex;
        align-items: center;
        gap: 12px;
        flex-wrap: wrap;
      }

      .promo-text svg {
        flex-shrink: 0;
        color: #fbbf24;
      }

      .promo-content strong {
        font-weight: 800;
        font-size: 17px;
        color: #fbbf24;
      }

      .promo-link {
        color: white;
        text-decoration: none;
        font-weight: 700;
        padding: 6px 16px;
        background: rgba(255, 255, 255, 0.15);
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 8px;
        transition: all 0.25s ease;
        white-space: nowrap;
      }

      .promo-link:hover {
        background: rgba(255, 255, 255, 0.28);
        transform: translateY(-1px);
      }

      /* Countdown Timer */
      .promo-countdown {
        display: flex;
        align-items: center;
        gap: 8px;
        flex-shrink: 0;
      }

      .countdown-box {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-width: 52px;
        padding: 6px 8px;
        background: rgba(0, 0, 0, 0.22);
        border: 1px solid rgba(255, 255, 255, 0.22);
        border-radius: 10px;
      }

      .countdown-num {
        font-size: 22px;
        font-weight: 800;
        line-height: 1;
        color: #fbbf24;
        font-family: 'Segoe UI', Arial, sans-serif;
        font-variant-numeric: tabular-nums;
      }

      .countdown-label {
        font-size: 10px;
        font-weight: 500;
        color: rgba(255, 255, 255, 0.85);
        margin-top: 4px;
      }

      .countdown-sep {
        font-size: 20px;
        font-weight: 800;
        color: rgba(255, 255, 255, 0.5);
        line-height: 1;
        padding-bottom: 14px;
      }

      .hero-improved {
        background: linear-gradient(135deg, #0f1419 0%, #1a1f2e 100%);
      }

      .hero-improved .hero-content-wrapper {
        background: transparent !important;
      }

      .hero-main-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 60px;
        align-items: center;
        padding: 80px 40px;
      }

      .hero-left-content {
        display: flex;
        flex-direction: column;
        gap: 24px;
      }

      .hero-badge {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: fit-content;
        padding: 8px 20px;
        background: rgba(64, 119, 243, 0.15);
        border: 1px solid rgba(64, 119, 243, 0.3);
        border-radius: 50px;
        color: #4077F3;
        font-size: 14px;
        font-weight: 500;
      }

      .hero-main-title {
        font-size: 48px;
        font-weight: 700;
        line-height: 1.2;
        color: #ffffff;
        margin: 0;
      }

      .hero-description {
        font-size: 18px;
        color: #b0b5c0;
        line-height: 1.6;
        margin: 0;
      }

      .hero-features {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 16px;
      }

      .feature-item {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px;
        background: rgba(64, 119, 243, 0.08);
        border-radius: 8px;
        color: #d0d5e0;
        font-size: 14px;
      }

      .feature-item svg {
        flex-shrink: 0;
      }

      .hero-guarantee-box {
        display: flex;
        align-items: center;
        gap: 12px;
        color: #8a8f9f;
        font-size: 14px;
      }

      .hero-guarantee-box svg {
        flex-shrink: 0;
        color: #4077F3;
      }

      .hero-stats-section {
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .stats-card {
        background: rgba(64, 119, 243, 0.1);
        border: 2px solid rgba(64, 119, 243, 0.25);
        border-radius: 20px;
        padding: 50px 45px;
        backdrop-filter: blur(10px);
        display: flex;
        flex-direction: column;
        gap: 40px;
        width: 100%;
        max-width: 450px;
      }

      .stat-box {
        display: flex;
        flex-direction: column;
        gap: 14px;
        text-align: center;
      }

      .stat-number {
        display: flex;
        align-items: baseline;
        justify-content: center;
        gap: 6px;
      }

      .counter,
      .counter-20m,
      .counter-380k {
        font-size: 52px;
        font-weight: 800;
        color: #4077F3;
        font-family: 'Segoe UI', Arial, sans-serif;
      }

      .stat-suffix {
        font-size: 28px;
        font-weight: 700;
        color: #4077F3;
        margin-left: 4px;
      }

      .stat-text {
        font-size: 16px;
        color: #b0b5c0;
        margin: 0;
        line-height: 1.5;
        font-weight: 500;
      }

      .hero-cta-inline {
        margin-top: 4px;
      }

      .hero-cta-btn-large {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 12px;
        padding: 18px 52px;
        background: linear-gradient(135deg, #4077F3 0%, #2E5CC8 100%);
        color: white;
        font-size: 20px;
        font-weight: 700;
        border-radius: 14px;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 12px 32px rgba(64, 119, 243, 0.45);
        border: none;
        cursor: pointer;
      }

      .hero-cta-btn-large:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 40px rgba(64, 119, 243, 0.5);
        background: linear-gradient(135deg, #5585F7 0%, #3A66D4 100%);
      }

      .hero-cta-btn-large:active {
        transform: translateY(-1px);
      }

      @media (max-width: 1024px) {
        .hero-main-grid {
          grid-template-columns: 1fr;
          gap: 40px;
          padding: 60px 30px;
        }

        .hero-main-title {
          font-size: 40px;
        }

        .stats-card {
          width: 100%;
          max-width: 400px;
        }
      }

      @media (max-width: 768px) {
        .promo-content {
          flex-direction: column;
          gap: 12px;
        }

        .promo-text {
          justify-content: center;
          text-align: center;
        }

        .promo-countdown {
          gap: 6px;
        }

        .countdown-box {
          min-width: 46px;
          padding: 5px 6px;
        }

        .countdown-num {
          font-size: 19px;
        }

        .hero-main-grid {
          padding: 40px 20px;
          gap: 30px;
        }

        .hero-main-title {
          font-size: 32px;
        }

        .hero-description {
          font-size: 16px;
        }

        .hero-features {
          grid-template-columns: 1fr;
        }

        .counter,
        .counter-20m,
        .counter-380k {
          font-size: 44px;
        }

        .stats-card {
          padding: 35px 30px;
          gap: 28px;
        }

        .hero-cta-inline {
          width: 100%;
        }

        .hero-cta-btn-large {
          padding: 16px 40px;
          font-size: 18px;
          width: 100%;
          max-width: 340px;
        }
      }
    </style>
    <!-- ===== END: Hero Section ===== -->
